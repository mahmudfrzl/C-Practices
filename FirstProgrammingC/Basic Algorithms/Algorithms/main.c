#include <stdio.h>
#include <stdlib.h>

int main()
{  /* a*x*x + b*x +c hellini tapmaq */
    /*
        float a ,b,c,x;
        float response;
        printf("Enter the numbers:\n");
        scanf("%f%f%f%f",&a,&b,&c,&x);
        response = a*x*x + b*x +c;
        printf("Return: %.2f",response);
        */

    /* Diktorgen prizmanin Taban alani: Yanal alanlar toplami toplam alani hacmini tapmaq*/
/*
 *     float a,b,h;
 *     float tabanAlan;
 *     float yanalAlan;
 *     float toplamAlan;
 *     float hacm;
 *
 *         printf("Prizmanin taraflarini girniz:\n");
 *         scanf("%f%f%f",&a,&b,&h);
 *         tabanAlan =  a*b;
 *         yanalAlan = 2*h*(a+b);
 *         toplamAlan = 2 * a* b;
 *         hacm = a* b *h;
 *         printf("Diktortgen prizmanin:\n");
 *         printf("Taban alani:%.2f\n",tabanAlan);
 *         printf("Yanal alanlar toplami:%.2f\n",yanalAlan);
 *         printf("Toplma alani:%.2f\n",toplamAlan);
 *         printf("Hacmi:%.2f\n",hacm);
 */

 /*Maksimum ve minimum sayini tapmaq */

/*         int n =0;
 *         int max =0;
 *         int min = 22200000;
 *         printf("Reqemleri daxil edin:\n");
 *         for(int i=0;i<7;i++){
 *             scanf("%d",&n);
 *          //    max = n >max ? n : max;
 *            // min = n <min ? n : min;
 *            max = n>max ? n : max;
 *            min = n< min ? n : min;
 *
 *         }
 *         printf("minum: %d, max:%d",min,max);
 */

/*         int n1,n2;
 *         printf("Please enter the numbers:\n");
 *         scanf("%d%d",&n1,&n2);
 *         if(n1==n2){
 *             printf("Iki reqem beraberdir");
 *         }else if(n1>n2){
 *             printf("Ilk reqem ikinciden boyukdur");
 *         }else{
 *         printf("Ikinci reqem boyukdur");
 *         }
 */
/*         int i =23;
 *         while(i>0){
 *             printf("%d ",i);
 *             i--;
 *
 *         }
 *
 */




}
